spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri:

server:
  port: 8088
eureka:
  client:
    serviceUrl:
      defaultZone: http://192.168.4.159:8761/eureka/